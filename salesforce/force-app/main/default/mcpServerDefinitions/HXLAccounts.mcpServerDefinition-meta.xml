<?xml version="1.0" encoding="UTF-8"?>
<McpServerDefinition xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>HXL Account Widget</description>
    <masterLabel>HXL-Accounts</masterLabel>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-GetAccountSummary</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>GetAccountSummary</operation>
        </apiDefinition>
        <descriptionOverride>Returns a summary of a Salesforce account: firmographics, owner, location, contact count, open pipeline totals, and the top open opportunities. Provide the Account Id (starts with 001). The result renders as an account summary card.</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>true</idempotent>
        <openWorld>false</openWorld>
        <readOnly>true</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>GetAccountSummaryapex_GetAccountSummary</toolName>
        <toolTitle>Get Account Summary</toolTitle>
        <uiResource>accountSummaryCard</uiResource>
    </tools>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-GetCloseReadiness</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>GetCloseReadiness</operation>
        </apiDefinition>
        <descriptionOverride>Checks whether an Opportunity can be closed: whether a signed order form is attached and which Closed Won fields still need values. Read-only. Call this first when a user wants to close a deal. Provide the Opportunity Id (starts with 006).</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>true</idempotent>
        <openWorld>false</openWorld>
        <readOnly>true</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>GetCloseReadinessapex_GetCloseReadiness</toolName>
        <toolTitle>Get Close Readiness</toolTitle>
        <uiResource>closeReadinessCard2</uiResource>
    </tools>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-SubmitClosedWon</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>SubmitClosedWon</operation>
        </apiDefinition>
        <descriptionOverride>Submits an Opportunity as Closed Won like the Close Opportunity screen flow. Call without confirm to validate and preview; the user reviews the card, then call again with confirm=true to write the finance and handoff fields, notify RevOps in Slack, and queue order form extraction. RevOps stamps the stage. Requires a signed order form on the record. Blank inputs keep existing values, and a blank LOB/Division defaults from the account type. When fields are missing, ask the user for all of them in one message rather than one at a time.</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>false</idempotent>
        <openWorld>false</openWorld>
        <readOnly>false</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>SubmitClosedWonapex_SubmitClosedWon</toolName>
        <toolTitle>Submit Closed Won</toolTitle>
        <uiResource>closeSubmissionCard2Won</uiResource>
    </tools>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-SubmitClosedLost</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>SubmitClosedLost</operation>
        </apiDefinition>
        <descriptionOverride>Closes an Opportunity as Closed Lost like the Close Opportunity screen flow. Call without confirm to preview: the first call starts the AI loss analysis in the background (about 30 seconds) and returns immediately; a later preview call shows the recommended reason, competitor, and narrative. The user reviews the card, then call again with confirm=true to stamp Closed Lost. Blank inputs keep existing values; blank notes default to the AI narrative; a blank LOB/Division defaults from the account type. When fields are missing, ask the user for all of them in one message rather than one at a time.</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>false</idempotent>
        <openWorld>false</openWorld>
        <readOnly>false</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>SubmitClosedLostapex_SubmitClosedLost</toolName>
        <toolTitle>Submit Closed Lost</toolTitle>
        <uiResource>closeSubmissionCard2Lost</uiResource>
    </tools>
    <resources>
        <resourceName>accountSummaryCard</resourceName>
        <resourceUri>ui://widget/lightningType/c__getAccountSummary</resourceUri>
        <resourceTitle>Account Summary Card</resourceTitle>
        <description>HXL widget that renders an account summary: header with industry and type, firmographics, owner and location, open pipeline with top opportunities, and a link to the record.</description>
    </resources>
    <resources>
        <resourceName>closeReadinessCard2</resourceName>
        <resourceUri>ui://widget/lightningType/c__getCloseReadiness2</resourceUri>
        <resourceTitle>Close Readiness Card</resourceTitle>
        <description>HXL widget showing whether an opportunity can be closed: status, signed order form check, Closed Won field checklist, and buttons to start Closed Won or Closed Lost.</description>
    </resources>
    <resources>
        <resourceName>closeSubmissionCard2Won</resourceName>
        <resourceUri>ui://widget/lightningType/c__submitClosedWon2</resourceUri>
        <resourceTitle>Closed Won Submission Card</resourceTitle>
        <description>HXL widget showing the preview, validation problems, or result of a Closed Won submission, with confirm and change buttons.</description>
    </resources>
    <resources>
        <resourceName>closeSubmissionCard2Lost</resourceName>
        <resourceUri>ui://widget/lightningType/c__submitClosedLost2</resourceUri>
        <resourceTitle>Closed Lost Submission Card</resourceTitle>
        <description>HXL widget showing the AI recommendation, preview, validation problems, or result of a Closed Lost submission, with confirm and change buttons.</description>
    </resources>
</McpServerDefinition>
